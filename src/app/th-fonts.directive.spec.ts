import { ThFontsDirective } from './th-fonts.directive';

describe('ThFontsDirective', () => {
  it('should create an instance', () => {
    const directive = new ThFontsDirective();
    expect(directive).toBeTruthy();
  });
});
